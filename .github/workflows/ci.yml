name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Workflow-level, not just on the build job: `svelte-kit sync` generates the
# types for `$env/static/public` from the environment, so without this
# svelte-check fails with "Module '$env/static/public' has no exported member
# PUBLIC_ENTRA_CLIENT_ID" even though the build would have been fine.
#
# The value is inert. The real client id is per-deployment config in Cloudflare
# Pages, and this app is client-side only with no server secrets.
env:
  PUBLIC_ENTRA_CLIENT_ID: 00000000-0000-0000-0000-000000000000

jobs:
  lint:
    name: eslint / prettier
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: 11

      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: eslint
        run: pnpm lint

      - name: prettier
        run: pnpm exec prettier --check .

  check:
    name: svelte-check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: 11

      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      # This is the job that actually type-checks. `pnpm build` does NOT - Vite
      # strips types without checking them, which is how 6 type errors and 86 lint
      # errors sat unnoticed behind a green build while TypeScript 7 had both tools
      # crashing. See the TypeScript pin in renovate.json.
      - name: svelte-check
        run: pnpm check

  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: 11

      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      # PUBLIC_ENTRA_CLIENT_ID comes from the workflow-level env above -
      # $env/static/public resolves at build time and Rollup fails outright
      # without it.
      - name: Build
        run: pnpm build

  # Single required check for the branch ruleset. Adding or renaming a job above
  # only needs updating here, not in the ruleset.
  ci-success:
    name: ci-success
    runs-on: ubuntu-latest
    if: always()
    needs: [lint, check, build]
    steps:
      - name: Fail if any required job did not pass
        if: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
        run: |
          echo "A required job failed or was cancelled: ${{ join(needs.*.result, ', ') }}"
          exit 1
      - name: All required jobs passed
        run: echo "ci-success — ${{ join(needs.*.result, ', ') }}"
